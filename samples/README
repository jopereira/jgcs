This package demonstrates how to build protocol independent applications
using JNDI. (*) denotes the file that needs to be changed to switch protocol
implementations.  The corresponding dependency should, in both cases, be
added to ivy.xml. By default, examples use JGroups.

Run each test as follows:

 1. Baseline: Hardcoded configuration
 
     Files:
       - src/net/sf/jgcs/test/HardcodedTest.java (*)
         
     Howto:
       - ant
       - ant run.hardcoded
       
 2. JNDI: Lookup dependencies in configuration files
 
     Files:
       - src/net/sf/jgcs/test/JNDITest.java
       - jndi.properties
       - config/default.properties (*)
                  
     Howto:
       - ant
       - ant run.jndi
